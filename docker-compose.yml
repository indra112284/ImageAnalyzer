services:
  image-analyzer:
    image: indrasena/image-analyzer:v1

    build:
      context: .
      dockerfile: Dockerfile

    container_name: image-analyzer

    volumes:
      - ./dataset:/app/dataset
      - ./processed:/app/processed
      - ./report:/app/report

    restart: "no"

    command: python main.py